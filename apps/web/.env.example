# Public browser configuration. Never place secrets in NEXT_PUBLIC_* variables.
# Local development defaults to http://localhost:8000 when this is empty.
# Vercel production and preview deployments must set an explicit API origin.
NEXT_PUBLIC_API_URL=
