{
  "name": "dcflens",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web"],
  "scripts": {
    "dev:web": "npm run dev --workspace @dcflens/web",
    "lint:web": "npm run lint --workspace @dcflens/web",
    "typecheck:web": "npm run typecheck --workspace @dcflens/web",
    "test:web": "npm run test --workspace @dcflens/web",
    "build:web": "npm run build --workspace @dcflens/web"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
